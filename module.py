class Robot:
    def __init__(self, name, color):
        # initialize properties code here
        pass

    def charge(self, amount):
        self.battery = self.battery + charge
        return f("{self.name}got {self.charge}")
    
    def repair(self,amount):
        self.repair
    
    def work(self, amount):
        self.work += int(amount)
        return self.work 
        
    def status():
        
        
    def recolor():
        
    def shutdown():
        self.shutdown = shutdown -
        if self.battery = 0:
            self.shutdown